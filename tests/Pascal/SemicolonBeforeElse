program SemicolonBeforeElse;
{ A ';' ending the THEN branch closes the whole IF in Pascal, so the ELSE
  that follows has nothing to attach to. The diagnostic should point at
  the ';' and say so. }
var
  n: Integer;
begin
  n := 3;
  if n < 2 then WriteLn('small');
  else WriteLn('large');
end.
